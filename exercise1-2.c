#include <stdio.h>

/* Exercise 1-2. Experiment to find out what happens when prints's argument string contains \c,
where c is some character not listed above. */

/*
Answer:

Error displays the filename, the function name and the line of error.
The error is displayed as an 'unknown escape sequence' and then the sequence that is the issue.
*/
